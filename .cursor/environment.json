{
  "name": "wp-security-dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "command -v composer >/dev/null 2>&1 && composer install || true",
  "terminals": []
}
